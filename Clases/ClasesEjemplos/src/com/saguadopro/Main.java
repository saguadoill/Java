package com.saguadopro;

/**
 * Metodo main por si se quiere probar los metodos de la clase Cuenta
 * <p>
 * 1. Crea una clase Cuenta con los métodos ingreso, reintegro y transferencia.
 * La clase contendrá un constructor por defecto, un constructor con parámetros,
 * un constructor copia y los métodos getters y setters.
 * <p>
 * 2. Crea una clase Contador con los métodos para incrementar y decrementar el contador.
 * La clase contendrá un constructor sin parámetros, un constructor con parámetros y los métodos getters y setters.
 * <p>
 * 3. Crea una clase Libro con los métodos préstamo, devolución y toString.
 * La clase contendrá un constructor sin parámetros, un constructor con parámetros y los métodos getters y setters.
 */
public class Main {

    public static void main(String[] args) {


    }
}
